[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: Unit tests
    integration: Integration tests that may require external systems
    slow: Tests that are expected to be slow
    api: Tests related to the API
    knowledge_graph: Tests related to the knowledge graph 